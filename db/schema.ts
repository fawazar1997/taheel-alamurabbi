import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const projectSettings = sqliteTable("project_settings", { id: integer("id").primaryKey(), projectName: text("project_name").notNull(), year: integer("year").notNull(), educatorMeetings: integer("educator_meetings").notNull().default(2), minDevelopmentMeetings: integer("min_development_meetings").notNull().default(2), minTrips: integer("min_trips").notNull().default(1), minWorkshops: integer("min_workshops").notNull().default(2), updatedAt: text("updated_at").notNull() });
export const organizations = sqliteTable("organizations", { id: text("id").primaryKey(), accessToken: text("access_token").notNull().unique(), name: text("name").notNull(), contactName: text("contact_name").notNull().default(""), phone: text("phone").notNull().default(""), email: text("email").notNull().default(""), participants: integer("participants").notNull().default(0), notes: text("notes").notNull().default(""), archived: integer("archived", { mode: "boolean" }).notNull().default(false), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() }, t=>[index("idx_organizations_archived").on(t.archived)]);
export const annualPlans = sqliteTable("annual_plans", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull(), year: integer("year").notNull(), developmentMeetings: text("development_meetings").notNull().default("[]"), trips: text("trips").notNull().default("[]"), workshops: text("workshops").notNull().default("[]"), evaluationFollowup: integer("evaluation_followup", { mode: "boolean" }), status: text("status").notNull().default("not_started"), submittedAt: text("submitted_at"), updatedAt: text("updated_at").notNull() }, t=>[uniqueIndex("idx_annual_plans_org_year").on(t.organizationId,t.year)]);
export const attendance = sqliteTable("attendance", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull(), meetingNumber: integer("meeting_number").notNull(), attendees: integer("attendees").notNull().default(0), updatedAt: text("updated_at").notNull() }, t=>[uniqueIndex("idx_attendance_org_meeting").on(t.organizationId,t.meetingNumber)]);
export const learningEnvironments = sqliteTable("learning_environments", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull(), name: text("name").notNull(), preScore: integer("pre_score"), postScore: integer("post_score"), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() });
